#pragma once
#include <steamworks/isteammatchmaking.h>

// using SteamInput_t = ISteamMatchmaking*(*)();
// using Init_t = void(*)(ISteamMatchmaking* self);
// using RunFrame_t = void(*)(ISteamMatchmaking* self);

// extern SteamInput_t _SteamInput;
// extern Init_t _Init;
// extern RunFrame_t _RunFrame;